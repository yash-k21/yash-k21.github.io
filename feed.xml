<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yash-k21.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yash-k21.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-02T08:46:01+00:00</updated><id>https://yash-k21.github.io/feed.xml</id><title type="html">blank</title><subtitle>Yash Khaitan&apos;s personal website </subtitle><entry><title type="html">Monte Carlo Integration</title><link href="https://yash-k21.github.io/blog/2026/MC-integration/" rel="alternate" type="text/html" title="Monte Carlo Integration"/><published>2026-07-02T00:00:00+00:00</published><updated>2026-07-02T00:00:00+00:00</updated><id>https://yash-k21.github.io/blog/2026/MC-integration</id><content type="html" xml:base="https://yash-k21.github.io/blog/2026/MC-integration/"><![CDATA[<h1 id="monte-carlo-integration">Monte Carlo Integration</h1> <p><em>Monte Carlo integration approximates an integral by averaging random evaluations of the integrand. I derive the estimator from scratch, show why its error shrinks like $1/\sqrt{N}$, and verify everything with a worked example.</em></p> <p>Suppose we want to compute a definite integral,</p> \[I = \int_a^b f(x)\, dx. \tag{1}\] <p>If $f$ has a closed-form antiderivative, we are done. But many interesting functions do not. A classic example is</p> \[f(x) = e^{-x^2}, \tag{2}\] <p>whose antiderivative cannot be written in terms of elementary functions. The standard workaround is deterministic numerical integration, such as the <a href="https://en.wikipedia.org/wiki/Trapezoidal_rule">trapezoidal rule</a>, where we evaluate $f$ on a fixed grid. This works well in one dimension but degrades quickly as the dimension of the problem grows.</p> <p>Monte Carlo integration takes a different approach. Rather than evaluating $f$ on a grid, we evaluate it at <em>random</em> points and average. At first glance this sounds like it should be worse. Why would randomness help? The answer rests on a single observation, which we can derive in a few lines.</p> <h2 id="integrals-are-expectations">Integrals are expectations</h2> <p>The key trick is to rewrite the integral in Equation 1 as an expectation. Multiply and divide the integrand by $b - a$:</p> \[I = \int_a^b f(x)\, dx = (b-a) \int_a^b f(x)\, \frac{1}{b-a}\, dx. \tag{3}\] <p>This looks like a pointless manipulation until we notice that $\frac{1}{b-a}$ is exactly the probability density function of a <a href="https://en.wikipedia.org/wiki/Continuous_uniform_distribution">uniform random variable</a> $U$ on $[a, b]$. So Equation 3 is really</p> \[I = (b-a)\, \mathbb{E}[f(U)], \qquad U \sim \text{Uniform}(a, b). \tag{4}\] <p>In words, the integral of $f$ is just the average value of $f$ under uniform sampling, scaled by the length of the interval. This reframing is the entire idea. Once an integral is an expectation, we can estimate it the way we estimate any expectation, with a sample mean.</p> <p>So draw $N$ <a href="https://en.wikipedia.org/wiki/Independent_and_identically_distributed_random_variables">independent and identically distributed</a> samples $U_1, \dots, U_N$ from $\text{Uniform}(a, b)$ and define the Monte Carlo estimator as</p> \[\hat{I}_N = \frac{b-a}{N} \sum_{i=1}^N f(U_i). \tag{5}\] <h2 id="why-it-works">Why it works</h2> <p>The first thing to check is that the estimator is <a href="https://en.wikipedia.org/wiki/Bias_of_an_estimator">unbiased</a>, meaning it is correct on average. By linearity of expectation,</p> \[\mathbb{E}[\hat{I}_N] = \frac{b-a}{N} \sum_{i=1}^N \mathbb{E}[f(U_i)] = (b-a)\, \mathbb{E}[f(U)] = I. \tag{6}\] <p>Furthermore, the <a href="https://en.wikipedia.org/wiki/Law_of_large_numbers">law of large numbers</a> tells us that a sample mean converges to its expectation, so $\hat{I}_N \to I$ as $N \to \infty$. Unbiasedness says we are right on average, and the law of large numbers says we get closer with more samples. But how fast?</p> <p>Since the $U_i$ are independent, the variance of the estimator is</p> \[\mathbb{V}[\hat{I}_N] = \frac{(b-a)^2}{N^2} \sum_{i=1}^N \mathbb{V}[f(U_i)] = \frac{(b-a)^2 \sigma_f^2}{N}, \tag{7}\] <p>where $\sigma_f^2 = \mathbb{V}[f(U)]$ is the variance of the integrand under uniform sampling. Taking a square root, the standard error is</p> \[\text{se}(\hat{I}_N) = \frac{(b-a)\, \sigma_f}{\sqrt{N}}. \tag{8}\] <p>The error therefore shrinks like $1/\sqrt{N}$​. Before moving on, Equation 8 deserves two comments.</p> <p>First, notice what is <em>not</em> in the formula. The rate does not depend on the dimension of the problem. If we integrate over a region in $\mathbb{R}^{100}$ rather than an interval, the error still shrinks like $1/\sqrt{N}$. Deterministic grid methods cannot say the same, since covering a high-dimensional space with a grid requires exponentially many points. This is why Monte Carlo dominates in high dimensions.</p> <p>Second, Equation 8 appears to have a problem. The standard error depends on $\sigma_f$, which we do not know. If we could compute expectations of $f$ under $U$ exactly, we would not need Monte Carlo in the first place. But notice that the same draws that estimate the integral also estimate $\sigma_f$, through the sample standard deviation of the $f(U_i)$ values. And since the <a href="https://en.wikipedia.org/wiki/Central_limit_theorem">central limit theorem</a> tells us $\hat{I}_N$ is approximately normal for large $N$, we immediately have a confidence interval,</p> \[\hat{I}_N \pm 1.96 \cdot \frac{(b-a)\, \hat{\sigma}_f}{\sqrt{N}}. \tag{9}\] <p>So a Monte Carlo estimate comes bundled with a measure of its own accuracy, at no extra cost. A deterministic grid method gives no such thing.</p> <h2 id="an-example">An example</h2> <p>Let’s verify all of this on Equation 2, integrating $e^{-x^2}$ over $[0, 1]$. The true value can be written using the <a href="https://en.wikipedia.org/wiki/Error_function">error function</a>,</p> \[I = \int_0^1 e^{-x^2}\, dx = \frac{\sqrt{\pi}}{2}\, \text{erf}(1) \approx 0.746824, \tag{10}\] <p>which gives us a ground truth to compare against. The estimator in Equation 5, along with the standard error in Equation 8, takes only a few lines of code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">mc_integrate</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="n">rng</span><span class="p">):</span>
    <span class="n">u</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">n</span><span class="p">)</span>
    <span class="n">fx</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">u</span><span class="p">)</span>
    <span class="n">est</span> <span class="o">=</span> <span class="p">(</span><span class="n">b</span> <span class="o">-</span> <span class="n">a</span><span class="p">)</span> <span class="o">*</span> <span class="n">fx</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>
    <span class="n">se</span> <span class="o">=</span> <span class="p">(</span><span class="n">b</span> <span class="o">-</span> <span class="n">a</span><span class="p">)</span> <span class="o">*</span> <span class="n">fx</span><span class="p">.</span><span class="nf">std</span><span class="p">(</span><span class="n">ddof</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">est</span><span class="p">,</span> <span class="n">se</span>
</code></pre></div></div> <p>Running it on our integrand,</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">scipy</span> <span class="kn">import</span> <span class="n">special</span>

<span class="n">f</span> <span class="o">=</span> <span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">x</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span>
<span class="n">truth</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">special</span><span class="p">.</span><span class="nf">erf</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>

<span class="n">est</span><span class="p">,</span> <span class="n">se</span> <span class="o">=</span> <span class="nf">mc_integrate</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">10_000</span><span class="p">,</span> <span class="n">rng</span><span class="o">=</span><span class="n">rng</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">estimate: </span><span class="si">{</span><span class="n">est</span><span class="si">:</span><span class="p">.</span><span class="mi">6</span><span class="n">f</span><span class="si">}</span><span class="s"> +/- </span><span class="si">{</span><span class="mf">1.96</span> <span class="o">*</span> <span class="n">se</span><span class="si">:</span><span class="p">.</span><span class="mi">6</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">truth:    </span><span class="si">{</span><span class="n">truth</span><span class="si">:</span><span class="p">.</span><span class="mi">6</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>estimate: 0.747045 +/- 0.003939
truth:    0.746824
</code></pre></div></div> <p>The estimate is within its own confidence interval of the truth, exactly as Equation 9 promised. But a single number hides the best part, so let’s watch the estimator work. We can compute the estimate and its confidence band after every new sample, which only requires cumulative sums:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">n_max</span> <span class="o">=</span> <span class="mi">5000</span>
<span class="n">u</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="n">n_max</span><span class="p">)</span>
<span class="n">fx</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">u</span><span class="p">)</span>
<span class="n">N</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">n_max</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
 
<span class="n">est_N</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">(</span><span class="n">fx</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span>  <span class="c1"># (b - a) = 1 here
</span><span class="n">var_N</span> <span class="o">=</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">(</span><span class="n">fx</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span> <span class="o">-</span> <span class="n">est_N</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">maximum</span><span class="p">(</span><span class="n">N</span> <span class="o">-</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
<span class="n">se_N</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">var_N</span> <span class="o">/</span> <span class="n">N</span><span class="p">)</span>
</code></pre></div></div> <p>Plotting the running estimate with its 95% band gives Figure 1.</p> <p><img src="/assets/img/blogs/MCIntegration/running_estimate.png" alt="Figure 1"/></p> <p><strong>Figure 1.</strong> A single Monte Carlo run, watched as it accumulates samples. The blue line is the estimate $\hat{I}_N$ after $N$ draws, the shaded region is the confidence band from Equation 9, and the dashed line is the true value. The x-axis is logarithmic so that both the early wobble and the late settling are visible.</p> <p>The figure confirms everything we derived. The estimate converges to $I$ (law of large numbers), the dashed line stays inside the band (Equation 9), and the band shrinks at the slow $1/\sqrt{N}$ rate, where each extra digit of accuracy costs $100$ times more samples.</p> <h2 id="conclusion">Conclusion</h2> <p>Monte Carlo integration rests on a single reframing, which is that an integral is an expectation in disguise, and expectations can be estimated by sample means. Everything else follows from standard results. Unbiasedness follows from linearity, consistency from the law of large numbers, and the $1/\sqrt{N}$ error rate from the variance of a sample mean.</p> <p>The lesson I take from this is not about integration. It is that a good reframing does the work of a good idea. One line of algebra in Equation 3 turned a calculus problem into an estimation problem, and estimation problems come with a complete toolkit attached.</p> <hr/> <p><strong>References</strong></p> <p>Metropolis, N., &amp; Ulam, S. (1949). The Monte Carlo method. <em>Journal of the American Statistical Association</em>, 44(247), 335-341.</p> <p>Robert, C. P., &amp; Casella, G. (2004). <em>Monte Carlo Statistical Methods</em>. Springer.</p>]]></content><author><name></name></author><category term="notes"/><category term="statistics"/><summary type="html"><![CDATA[The integral you cannot solve is an average you can estimate.]]></summary></entry></feed>